"""Empty admin for `lab` app.

The app is intentionally static for now; registering models is omitted
so the app can be extracted later without admin dependencies.
"""
from django.contrib import admin


# Intentionally left blank — no model registration
