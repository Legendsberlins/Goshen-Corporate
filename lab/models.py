"""Placeholder models module for `lab` app.

This app is intentionally static for now; models were removed so the
app can be extracted later without model/migration dependencies.
"""

from django.db import models


# No models defined intentionally.
